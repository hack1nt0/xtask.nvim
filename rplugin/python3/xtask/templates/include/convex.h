#ifndef INCLUDED_CONVEX_H
#define INCLUDED_CONVEX_H

template<typename T>
struct Convex {
    
    bool insert(T x, T y) {
        
    }

    //k: slope
    //return: point index i, s.t. arg_i^minimum b = y_i - k * x_i
    int operator*(T k) {
    }
    
}

#endif
